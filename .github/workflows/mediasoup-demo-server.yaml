name: mediasoup-demo-server

on:
  push:
    branches: [v3]
    paths:
      - 'server/**'
      - '.github/workflows/mediasoup-demo-server.yaml'
  pull_request:
    paths:
      - 'server/**'
      - '.github/workflows/mediasoup-demo-server.yaml'
  workflow_dispatch:

concurrency:
  # Cancel a currently running workflow from the same PR, branch or tag when a
  # new workflow is triggered.
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    strategy:
      matrix:
        ci:
          - os: ubuntu-22.04
            node: 22
          - os: ubuntu-24.04
            node: 24
          - os: ubuntu-26.04
            node: 26
          - os: macos-14
            node: 22
          - os: macos-15
            node: 24
          - os: windows-2025
            node: 26

    runs-on: ${{ matrix.ci.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.ci.node }}

      - name: Configure cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.npm
          key: ${{ matrix.ci.os }}-node-${{ hashFiles('server/package.json') }}
          restore-keys: |
            ${{ matrix.ci.os }}-node-

      - name: npm ci --prefix server --foreground-scripts
        run: npm ci --prefix server --foreground-scripts

      - name: npm audit --omit dev --prefix server
        run: npm audit --omit dev --prefix server

      - name: npm run lint --prefix server
        run: npm run lint --prefix server

      - name: npm run typescript:build --prefix server
        run: npm run typescript:build --prefix server

