# TODO mediasoup-demo-broadcaster

- Unify API messages so `server` and `mediasoup-demo-broadcaster` import them from same file using npm workspaces.
- User configurable log level for ffmpeg and gstreamer somehow.
- CI.
